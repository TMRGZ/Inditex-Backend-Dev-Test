spring:
  main:
    web-application-type: reactive
endpoints:
  existing-api:
    url: ${endpoints.existing-api.givenUrl:${endpoints.existing-api.formed-url}}
    formed-url: ${endpoints.existing-api.base-path}:${endpoints.existing-api.port}
    base-path: http://localhost
    port: ${wiremock.server.port}